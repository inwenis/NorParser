using System.Collections.Generic;

namespace Logic
{
    public class Parser
    {
        public List<object> Parse(string input)
        {
            var list = new List<object>();
            list.Add(new object());
            return list;
        }
    }
}